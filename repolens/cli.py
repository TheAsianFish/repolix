"""
cli.py

Command-line interface for repolens. Exposes subcommands for indexing
a repository and querying the resulting index via natural language.
"""

# TODO: Implement Click-based CLI with `index` and `query` subcommands
# that wire together walker, chunker, embedder, store, and retriever.
